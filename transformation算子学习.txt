map, filter, flatMap算子学习

Java 的FlatMapFunction接口中的call方法，在spark 1.x版本返回的是一个可迭代的集合，而在spark 2.x
版本中返回的是一个迭代器


groupByKey, reduceByKey, sortByKey算子学习


join, cogroup, union 片子学习


intersection, distinct, cartesian 算子学习

	intersection, 交集
	distinct 去重
	cartesian 求笛卡尔集
mapPartitions, reparation, coalesce 算子学习
	
	mapPartition 
	reparation， coalesce 重新进行分区 窄依赖， 宽依赖 shuffle
	
	reparation分区会进行shuffle操作，等同于shuffle为true的coalesce
	
	rdd.coalesce(m, shuffle)
		m:  新的分区数
		shuffler： 是否shuffle
		
		N: 代表原来的分区数据
		M: 新的分区数据
			1. N < M 需要将shuffle 设置为true.
			2. N > M 相差不多， N=1000 M=100 建议shuffle=false, 父RDD和子RDD是窄依赖
			3. N >> M 比如N=100 M=1 建议shuffle设置为true，这样性能更好。
				设置为false,父RDD和子RDD是窄依赖，它们同在一个stage中。造成任务的并行度不够，从
				而速度缓慢。
		
	
	
	
filter 过滤之后， partition内的数据量会减少,应重新分区
	
